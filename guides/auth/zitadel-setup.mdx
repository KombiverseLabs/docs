---
title: Zitadel setup
description: Configure Zitadel OIDC provider
---

# Zitadel setup

Configure Zitadel as your OIDC provider.

## Installation

```bash
# Docker Compose
docker run -d \
  --name zitadel \
  -p 8080:8080 \
  ghcr.io/zitadel/zitadel:latest
```

## Configuration

1. Create organization
2. Create application
3. Configure redirect URIs
4. Get client credentials

## Integration

```env
ZITADEL_URL=https://zitadel.example.com
ZITADEL_CLIENT_ID=your-client-id
ZITADEL_CLIENT_SECRET=your-client-secret
```
