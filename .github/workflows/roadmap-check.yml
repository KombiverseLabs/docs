name: roadmap-check

on:
  pull_request:
    paths:
      - "ROADMAP.md"
  push:
    branches: [main]
    paths:
      - "ROADMAP.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  roadmap-check:
    uses: KombiverseLabs/.github/.github/workflows/roadmap-check.yml@main
    secrets: inherit