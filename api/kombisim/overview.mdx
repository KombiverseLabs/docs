---
title: KombiSim API overview
description: REST API reference for KombiSim simulation engine
---

# KombiSim API

KombiSim provides a REST API for creating and managing infrastructure simulations.

**Base URL:** `http://localhost:5270/api/v1`

**Authentication:** None (local deployment)

## API endpoints

<CardGroup cols={2}>\n  <Card title=\"Simulations\" icon=\"flask\" href=\"/api/kombisim/simulations\">\n    Create and manage simulations\n  </Card>\n  <Card title=\"Nodes\" icon=\"server\" href=\"/api/kombisim/nodes\">\n    Manage simulated nodes\n  </Card>\n  <Card title=\"Templates\" icon=\"file-code\" href=\"/api/kombisim/templates\">\n    Browse and apply templates\n  </Card>\n</CardGroup>

## Quick start

### Create simulation

```bash
curl -X POST http://localhost:5270/api/v1/simulations \
  -H "Content-Type: application/json" \
  -d '{\
    "name": "my-test",\
    "nodes": [\
      {"name": "web-server", "location": "local"},\
      {"name": "database", "location": "local"}\
    ]\
  }'
```

### Start simulation

```bash
curl -X POST http://localhost:5270/api/v1/simulations/{id}/start
```

### Get SSH details

```bash
curl http://localhost:5270/api/v1/nodes/{id}/ssh
```

## API structure

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/v1/health` | GET | Health check |
| `/api/v1/simulations` | GET, POST | List/create simulations |
| `/api/v1/simulations/{id}` | GET, DELETE | Manage simulation |
| `/api/v1/simulations/{id}/start` | POST | Start all nodes |
| `/api/v1/simulations/{id}/stop` | POST | Stop all nodes |
| `/api/v1/nodes` | GET | List all nodes |
| `/api/v1/nodes/{id}/ssh` | GET | Get SSH connection details |
| `/api/v1/templates` | GET | List templates |
| `/api/v1/templates/{id}/apply` | POST | Apply template |

## Next steps

<CardGroup cols={2}>\n  <Card title=\"Simulations API\" icon=\"flask\" href=\"/api/kombisim/simulations\">\n    Simulation management\n  </Card>\n  <Card title=\"Nodes API\" icon=\"server\" href=\"/api/kombisim/nodes\">\n    Node management\n  </Card>\n</CardGroup>
