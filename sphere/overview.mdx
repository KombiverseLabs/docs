---
title: kombify Sphere
description: Manage your homelabs from anywhere — no setup required
sidebarTitle: Overview
icon: globe
---

# kombify Sphere

**kombify Sphere** is the easiest way to manage your homelab. Sign up, connect your servers, and you're done — we handle the rest.

## The simple version

<Card title="What is Sphere?" icon="lightbulb">
  Sphere is like having a professional IT team for your homelab. It provides a web dashboard where you can manage all your servers, deploy services, and invite team members — without setting up any infrastructure yourself.
</Card>

## Self-hosted vs. managed

<Tabs>
  <Tab title="Self-hosted (Stack only)">
    **You manage everything:**
    - ✅ Complete control
    - ✅ No subscription costs
    - ❌ You handle updates and security patches
    - ❌ You configure backups
    - ❌ You set up user management
    - ❌ One homelab at a time
  </Tab>
  <Tab title="Managed (Sphere)">
    **We handle the complexity:**
    - ✅ Automatic updates and security
    - ✅ Built-in backups
    - ✅ User management and SSO
    - ✅ Manage multiple homelabs from one place
    - ✅ Team collaboration built-in
    - ✅ Support when you need it
  </Tab>
</Tabs>

<Accordion title="🔧 Technical details" icon="code">
  Sphere is a multi-tenant SaaS platform built with:
  - **Frontend:** SvelteKit with real-time updates
  - **Backend:** Go API with PocketBase for state
  - **Identity:** Zitadel for OIDC/SAML/passkey authentication
  - **Billing:** Stripe for subscriptions
  - **Agent:** Lightweight Go binary with mTLS for secure homelab connectivity
  
  Your data stays on your servers — Sphere only stores configuration and metadata.
</Accordion>

## How it works

```mermaid
flowchart TB
    subgraph You["👤 You & Your Team"]
        Admin["Admin"]
        Dev["Developer"]
        Viewer["Viewer"]
    end
    
    subgraph Sphere["☁️ kombify Sphere"]
        Dashboard["🖥️ Dashboard"]
        Auth["🔐 Login<br/><small>Passkeys, SSO</small>"]
        API["🔌 API"]
        Billing["💳 Billing"]
    end
    
    subgraph Homelabs["🏠 Your Homelabs"]
        HL1["<b>Production</b><br/>3 nodes"]
        HL2["<b>Development</b><br/>1 node"]
        HL3["<b>Testing</b><br/>2 nodes"]
    end
    
    You -->|"Secure login"| Auth
    Auth --> Dashboard
    Dashboard --> API
    API --> Billing
    API <-->|"Encrypted tunnel"| Homelabs
    
    style You fill:#dbeafe,stroke:#2563eb
    style Sphere fill:#fef3c7,stroke:#d97706
    style Homelabs fill:#d1fae5,stroke:#059669
```

### What stays where?

| Data | Location | Why |
|------|----------|-----|
| **Your files & databases** | Your servers | Your data never leaves your infrastructure |
| **Configuration** | Sphere cloud | So you can access it from anywhere |
| **User accounts** | Sphere cloud | Managed securely by Zitadel |
| **Deployment history** | Sphere cloud | Track who changed what, when |

## Key features

<CardGroup cols={3}>
  <Card title="Multi-homelab" icon="server">
    Manage production, development, and testing environments from one dashboard
  </Card>
  <Card title="Team access" icon="users">
    Invite team members with role-based permissions
  </Card>
  <Card title="Passwordless login" icon="fingerprint">
    Passkeys and SSO — no passwords to remember
  </Card>
  <Card title="Automatic backups" icon="cloud-arrow-up">
    Configuration backed up automatically
  </Card>
  <Card title="Usage insights" icon="chart-line">
    See how your infrastructure is performing
  </Card>
  <Card title="Priority support" icon="headset">
    Get help when you need it
  </Card>
</CardGroup>

## Architecture

```mermaid
graph TB
    subgraph Cloud["☁️ Sphere Cloud"]
        subgraph Frontend["Frontend"]
            SvelteKit["🖥️ Dashboard<br/><small>SvelteKit</small>"]
        end
        
        subgraph Backend["Backend"]
            API["🔌 API<br/><small>Go</small>"]
            Auth["🔐 Identity<br/><small>Zitadel</small>"]
            PB["💾 State<br/><small>PocketBase</small>"]
        end
        
        subgraph Integrations["Integrations"]
            Stripe["💳 Payments<br/><small>Stripe</small>"]
            SMTP["📧 Email"]
        end
    end
    
    subgraph OnPrem["🏠 Your Premises"]
        Agent1["🤖 Agent<br/><small>Homelab 1</small>"]
        Agent2["🤖 Agent<br/><small>Homelab 2</small>"]
    end
    
    SvelteKit --> API
    API --> Auth
    API --> PB
    API --> Stripe
    API --> SMTP
    
    API <-->|"mTLS encrypted"| Agent1
    API <-->|"mTLS encrypted"| Agent2
    
    style Cloud fill:#f0f9ff,stroke:#0369a1
    style OnPrem fill:#d1fae5,stroke:#059669
```

<Accordion title="🔧 How the agent connection works" icon="link">

```mermaid
sequenceDiagram
    participant Homelab as Your Server
    participant Agent as Sphere Agent
    participant Sphere as Sphere Cloud
    
    Note over Agent,Sphere: Initial Setup (once)
    Agent->>Sphere: Register with pairing token
    Sphere->>Agent: Issue mTLS certificate
    
    Note over Agent,Sphere: Normal Operation
    loop Every 30 seconds
        Agent->>Sphere: Heartbeat + metrics
        Sphere->>Agent: Any pending commands?
    end
    
    Note over Agent,Sphere: Deployment
    Sphere->>Agent: Deploy command
    Agent->>Homelab: Execute deployment
    Agent->>Sphere: Report status
```

**Key points:**
- Agent connects **outbound** — no inbound ports needed on your network
- All communication is **mTLS encrypted** — both sides verify each other
- Agent is a **lightweight Go binary** (~15MB) that runs as a systemd service

</Accordion>
    Agent->>Homelab: Execute deployment
    Agent->>Sphere: Report status
```

### Agent Installation

<Steps>
  <Step title="Get pairing token">
    In Sphere dashboard, go to **Homelabs → Add Homelab** and copy your pairing token.
  </Step>
  
  <Step title="Install agent">
    ```bash
    curl -fsSL https://get.kombify.dev/sphere-agent | bash
    ```
  </Step>
  
  <Step title="Configure agent">
    ```bash
    sphere-agent init --token YOUR_PAIRING_TOKEN
    ```
  </Step>
  
  <Step title="Start agent">
    ```bash
    sudo systemctl enable --now sphere-agent
    ```
  </Step>
</Steps>

## Team & Access Control

### Role-Based Access

| Role | View | Deploy | Configure | Billing | Admin |
|------|:----:|:------:|:---------:|:-------:|:-----:|
| Viewer | ✅ | ❌ | ❌ | ❌ | ❌ |
| Operator | ✅ | ✅ | ❌ | ❌ | ❌ |
| Developer | ✅ | ✅ | ✅ | ❌ | ❌ |
| Admin | ✅ | ✅ | ✅ | ✅ | ✅ |
| Owner | ✅ | ✅ | ✅ | ✅ | ✅ |

### SSO Configuration

Sphere uses Zitadel for enterprise-grade authentication:

```mermaid
sequenceDiagram
    participant User
    participant Sphere
    participant Zitadel
    participant IdP as Your IdP (Optional)
    
    User->>Sphere: Access dashboard
    Sphere->>Zitadel: Redirect to login
    
    alt Native Login
        Zitadel->>User: Show login form
        User->>Zitadel: Enter credentials
    else Enterprise SSO
        Zitadel->>IdP: Redirect to corporate IdP
        User->>IdP: Authenticate
        IdP->>Zitadel: Return assertion
    end
    
    Zitadel->>Sphere: Return tokens
    Sphere->>User: Authenticated session
```

Supported identity providers:
- **OIDC**: Any OIDC-compliant provider
- **SAML**: Okta, Azure AD, OneLogin, etc.
- **LDAP**: Active Directory, OpenLDAP
- **Social**: GitHub, Google, Microsoft

## Pricing Plans

<CardGroup cols={3}>
  <Card title="Starter" icon="seedling">
    **Free**
    
    - 1 homelab
    - 5 nodes max
    - Community support
    - 7-day backup retention
  </Card>
  
  <Card title="Pro" icon="rocket">
    **$19/month**
    
    - 3 homelabs
    - Unlimited nodes
    - Priority support
    - 30-day backup retention
    - Custom domain
  </Card>
  
  <Card title="Team" icon="building">
    **$49/month**
    
    - 10 homelabs
    - Unlimited nodes
    - 5 team members
    - SSO integration
    - 90-day backup retention
    - SLA guarantee
  </Card>
</CardGroup>

<Note>
  All plans include a 14-day free trial. No credit card required.
</Note>

## Dashboard Features

### Homelab Overview

View all your homelabs at a glance:
- Node health status
- Resource utilization
- Recent deployments
- Active alerts

### Deployment History

Track all changes:
- Who deployed what
- When it was deployed
- Rollback capability
- Diff viewer

### Monitoring & Alerts

Built-in monitoring:
- Node availability
- Resource usage
- Service health
- Custom alert rules

## Security

<AccordionGroup>
  <Accordion title="Data Encryption" icon="lock">
    - All data encrypted at rest (AES-256)
    - All traffic encrypted in transit (TLS 1.3)
    - mTLS for agent communication
  </Accordion>
  
  <Accordion title="Access Control" icon="shield">
    - Role-based access control
    - Audit logging for all actions
    - IP allowlist support (Team plan)
  </Accordion>
  
  <Accordion title="Compliance" icon="clipboard-check">
    - SOC 2 Type II (in progress)
    - GDPR compliant
    - Data residency options (EU/US)
  </Accordion>
</AccordionGroup>

## Self-Hosted vs Sphere

| Feature | Self-Hosted (Stack) | Managed (Sphere) |
|---------|---------------------|------------------|
| **Cost** | Free | $0-49/month |
| **Setup** | ~1 hour | ~5 minutes |
| **Updates** | Manual | Automatic |
| **Multi-homelab** | No | Yes |
| **Team access** | DIY | Built-in |
| **SSO** | DIY | Included |
| **Backups** | DIY | Automatic |
| **Support** | Community | Priority |

## Getting Started

<Steps>
  <Step title="Create account">
    Sign up at [sphere.kombify.dev](https://sphere.kombify.dev)
  </Step>
  
  <Step title="Add your first homelab">
    Follow the guided setup to connect your infrastructure
  </Step>
  
  <Step title="Deploy your first stack">
    Choose a StackKit and deploy in minutes
  </Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Quickstart Guide" icon="rocket" href="/sphere/quickstart">
    Get up and running in 5 minutes
  </Card>
  <Card title="Agent Installation" icon="download" href="/sphere/agent">
    Detailed agent setup instructions
  </Card>
  <Card title="SSO Setup" icon="key" href="/sphere/sso">
    Configure enterprise authentication
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/sphere">
    Automate with the Sphere API
  </Card>
</CardGroup>
