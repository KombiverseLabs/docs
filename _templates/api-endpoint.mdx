---
title: "{HTTP Method} {/path}"
description: "What this endpoint does"
---

{One sentence describing what this endpoint does.}

## Request

<ParamField path="param_name" type="string" required>
  Description of this path parameter.
</ParamField>

<ParamField body="field_name" type="string" required>
  Description of this body field.
</ParamField>

<ParamField body="optional_field" type="number">
  Description of this optional field. Default: `0`.
</ParamField>

<ParamField query="page" type="number">
  Page number for pagination. Default: `1`.
</ParamField>

## Response

<ResponseField name="id" type="string">
  Unique identifier.
</ResponseField>

<ResponseField name="status" type="string">
  Current status. One of: `active`, `pending`, `failed`.
</ResponseField>

<ResponseField name="created_at" type="string">
  ISO 8601 timestamp of creation.
</ResponseField>

## Examples

<RequestExample>
```bash cURL
curl -X POST https://api.example.com/v1/resource \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "field_name": "value",
    "optional_field": 5
  }'
```

```javascript JavaScript
const response = await fetch('https://api.example.com/v1/resource', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    field_name: 'value',
    optional_field: 5,
  }),
});
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "res_abc123",
  "status": "active",
  "created_at": "2025-01-15T10:30:00Z"
}
```

```json 400
{
  "error": {
    "code": "validation_error",
    "message": "field_name is required"
  }
}
```
</ResponseExample>
