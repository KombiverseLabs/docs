---
title: StackKits Quickstart
description: Use your first StackKit in 5 minutes
sidebarTitle: Quickstart
icon: rocket
---


Learn to use StackKits by creating a simple homelab configuration.

## Step 1: Choose a StackKit

Available StackKits:

| Kit | Use Case | Complexity |
|-----|----------|------------|
| `base-kit` | Single node, beginners | Low |
| `ha-kit` | High availability | Medium |
| `modern-homelab` | Multi-node Docker + Coolify | High |

For this quickstart, we'll use `base-kit`.

## Step 2: Create kombination.yaml

```yaml
# kombination.yaml
stackkit: base-kit

domain: homelab.local
email: you@example.com

ssl:
  provider: selfsigned

services:
  traefik:
    enabled: true
  portainer:
    enabled: true
  homepage:
    enabled: true
```

## Step 3: Validate Configuration

```bash
kombify validate
```

Expected output:
```
✓ Configuration valid
✓ All service dependencies satisfied
✓ No port conflicts detected

Ready to deploy!
```

## Step 4: Generate Infrastructure

```bash
kombify generate
```

This creates:
- `docker-compose.yml`
- Service configuration files
- TLS certificates (if self-signed)

## Step 5: Deploy

```bash
docker compose up -d
```

## What You Get

After deployment:

| Service | URL |
|---------|-----|
| Traefik Dashboard | `https://traefik.homelab.local` |
| Portainer | `https://portainer.homelab.local` |
| Homepage | `https://home.homelab.local` |

## Next Steps

<CardGroup cols={2}>
  <Card title="Base Kit details" icon="house" href="/stackkits/kits/base-kit">
    Full documentation for the Base Kit
  </Card>
  <Card title="Add Services" icon="plus">
    Expand your homelab with more services
  </Card>
</CardGroup>
